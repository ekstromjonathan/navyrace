# Used only if a Railway service has rootDirectory = pt.
# The lodd.ai service builds from repo root via /railway.toml + Dockerfile.

[build]
builder = "RAILPACK"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ALWAYS"
